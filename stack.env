# Enabled service profiles
# Remove a profile to disable that group of services, commit the change.
#
#   dev        - gitlab, gitlab-runner, nexus, registry
#   monitoring - prometheus, grafana, loki, promtail
#   storage    - minio
#   security   - vault
#   infra      - portainer
#
# Always on (no profile): traefik, homepage, watchtower

COMPOSE_PROFILES=dev,monitoring,storage,security,infra

# TLS profile: local (mkcert), cloud (self-signed), aws (ACM certs + real domain)
TLS_PROFILE=local
